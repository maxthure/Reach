# Reach

Components:

- `backend`: The core processing unit of Reach.

- `device-doorman` : 

- `device-simulator`:

- `web-frontend`:

- `documentation`: The documentation of each of the components. 


